interface:
  display_name: "Reference Design Workflow"
  short_description: "Analyze references, then run Product Design"
  default_prompt: "Use $reference-design-workflow with the attached references, then route through Product Design to create the requested image or HTML prototype."
